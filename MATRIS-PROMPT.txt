================================================================================
 MATRIS — PROMPT ATT KOPIERA
 Klistra in allt under linjen i Lovable-chatten. Två uppgifter, i ordning.
================================================================================

--------------------------------------------------------------------------------
 KLISTRA IN FRAN HAR OCH NER
--------------------------------------------------------------------------------

Two tasks, in order. Report back after the first.

1. CHECK WHETHER THE COMPATIBILITY CHECKS WORK

On the build page, the card "Do these parts fit together?" shows a verdict per
check. I set the panel count to 14 with JA Solar JAM72D30-550/MB and Deye
SUN-6K-SG04LP1-EU. Fourteen in series is about 699 V, above that inverter's
500 V DC limit. Every check still said "Looks OK".

Is that correct, or a bug? If it is a bug, fix it first and explain what was
wrong. Change nothing else while you look.

2. ADD A TABLE SHOWING WHICH COMBINATIONS FIT TOGETHER

Today the page confirms one combination at a time, so a user cannot see which
parts work with which.

Place it on the build page below the "Do these parts fit together?" card,
collapsed behind a "Show all combinations" toggle, so it does not overwhelm a
beginner.

The table:
- Rows: panels, grouped by identical valid range, labelled with wattage and
  count, e.g. "740 W (43 models)". 61 rows is not readable.
- Columns: one per inverter, using its short name.
- Cells: the panel-count range that works, e.g. "4-8". Nothing works: a dash,
  with the reason in a tooltip.
- Sweep panel counts from 1 to 30.
- One row below for the battery: maximum batteries per inverter.

Beneath the table, one sentence naming what limits the narrowest and the widest
range, in plain words. No internal rule keys or voltages in the table.

Build it by extracting a helper from the existing evaluation that answers: for
this panel, this inverter and this count, which checks pass? Use that helper in
both the existing card and the new table, so the two can never disagree. Do not
write a second copy of the rules. If the logic must be restructured to allow
that, tell me instead of duplicating it.

Match the reading level of the rest of the page. Change no existing behaviour.

When you are done: list the files you changed, and paste the helper.

--------------------------------------------------------------------------------
 SLUT
--------------------------------------------------------------------------------
